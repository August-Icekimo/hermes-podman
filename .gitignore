# 機密
.env
.env.bak.*
.env.*.bak
worker/.env
legacy-podman/worker/.env

# VM 佈署產物
# （.gitignore 不支援行尾註解，說明一律寫在上一行）
# 下載的 cloud image
vm/.cache/
# 產生的 cloud-init seed，user-data 內含 LINE 憑證明文
vm/.build/

# 對接 secure-gateway 的交接文件。刻意不進版控：內含 VM 的 MAC、tailnet IP
# 與子網域命名，而這個 repo 是 public。檔案本身留在 docs/ 供本機參考。
docs/secure-gateway-handoff.md
